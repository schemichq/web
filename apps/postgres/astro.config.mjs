// @ts-check
import { defineConfig } from "astro/config";

// postgres.schemic.dev - the PostgreSQL driver site. Driver is in the works;
// this is a placeholder scaffold for now.
export default defineConfig({
  site: "https://postgres.schemic.dev",
});
