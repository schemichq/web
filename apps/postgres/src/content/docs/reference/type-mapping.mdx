---
title: Type mapping
description: The complete mapping from the s schema builders to Postgres column types, with support status — scalars, pg-native parameterized types, arrays, json, and the modifiers.
---

This is the exhaustive matrix from each `s` builder to the Postgres column it emits, with a support status. The `s` vocabulary is Postgres-native: alongside the Zod-style builders (`s.string`, `s.number`) there are precise pg types (`text`, `varchar`, `numeric`, `timestamptz`, …). For how types compose into DDL see [from schema to DDL](/docs/concepts/schema-to-ddl).

## Text

<MappingTable targetLabel="Postgres" rows={[
  { sz: "s.text() / s.string()", type: "text", status: "ok" },
  { sz: "s.varchar(n)", type: "varchar(n)", status: "ok", notes: "Length preserved; also validated app-side." },
  { sz: "s.char(n)", type: "char(n)", status: "ok" },
  { sz: "s.enum([...])", type: "text", status: "partial", notes: "App-side Zod enum; projects to text. For a native CREATE TYPE enum, use defineEnum." },
  { sz: "s.literal(v)", type: "text / double precision / boolean", status: "ok", notes: "Column type follows the literal's JS type." },
  { sz: "s.citext()", type: "citext", status: "partial", notes: "Case-insensitive text; requires the citext extension (declare via defineExtension) — the embedded PGlite engine may not bundle it." },
  { sz: "s.email() / s.url() / s.cuid2() / …", type: "text", status: "ok", notes: "String-format factories (email, url, emoji, nanoid, cuid, cuid2, ulid, guid, xid, ksuid, base64, base64url, e164, jwt) — app-side Zod validation; the column stays text." }
]} />

## Numeric

<MappingTable targetLabel="Postgres" rows={[
  { sz: "s.smallint()", type: "smallint", status: "ok" },
  { sz: "s.integer() / s.int()", type: "integer", status: "ok" },
  { sz: "s.bigint()", type: "bigint", status: "partial", notes: "App value is a JS number today — values above 2^53 lose precision; a precise bigint fix is queued." },
  { sz: "s.serial()", type: "integer (identity)", status: "ok", notes: "Modeled as GENERATED BY DEFAULT AS IDENTITY." },
  { sz: "s.bigserial()", type: "bigint (identity)", status: "partial", notes: "Same number-precision caveat as s.bigint() (values above 2^53)." },
  { sz: "s.numeric(p, s) / s.decimal(p, s)", type: "numeric(p, s)", status: "ok", notes: "Precision and scale preserved." },
  { sz: "s.numeric()", type: "numeric", status: "ok" },
  { sz: "s.real()", type: "real", status: "ok" },
  { sz: "s.doublePrecision() / s.float() / s.number()", type: "double precision", status: "ok" },
  { sz: "s.money()", type: "money", status: "ok", notes: "String app-side." }
]} />

## Boolean & temporal

<MappingTable targetLabel="Postgres" rows={[
  { sz: "s.boolean() / s.bool()", type: "boolean", status: "ok" },
  { sz: "s.timestamptz()", type: "timestamp with time zone", status: "ok", notes: "Date app-side." },
  { sz: "s.timestamp()", type: "timestamp", status: "ok", notes: "Without time zone; Date app-side." },
  { sz: "s.date()", type: "date", status: "ok" },
  { sz: "s.time()", type: "time", status: "ok", notes: "String app-side." },
  { sz: "s.timetz()", type: "timetz", status: "ok", notes: "String app-side." },
  { sz: "s.interval()", type: "interval", status: "ok", notes: "String app-side." }
]} />

## Identifier, network & binary

<MappingTable targetLabel="Postgres" rows={[
  { sz: "s.uuid()", type: "uuid", status: "ok" },
  { sz: "s.bytea()", type: "bytea", status: "ok", notes: "Uint8Array app-side." },
  { sz: "s.inet()", type: "inet", status: "ok" },
  { sz: "s.cidr()", type: "cidr", status: "ok" },
  { sz: "s.macaddr()", type: "macaddr", status: "ok" }
]} />

## JSON & structural

<MappingTable targetLabel="Postgres" rows={[
  { sz: "s.jsonb(shape?)", type: "jsonb", status: "ok", notes: "Opaque on disk; sub-structure validated app-side by Zod." },
  { sz: "s.json(shape?)", type: "json", status: "partial", notes: "Round-trips; distinct from jsonb." },
  { sz: "s.object({ ... })", type: "jsonb", status: "ok", notes: "Collapses to one opaque jsonb column." },
  { sz: "s.array(el) / T[] / .array()", type: "el[]", status: "partial", notes: "Round-trips for canonical element types; arrays of pg-native element types are a known gap." }
]} />

## Foreign keys

<MappingTable targetLabel="Postgres" rows={[
  { sz: "s.references(table, opts?)", type: "text + FOREIGN KEY", status: "ok", notes: "A text column referencing table(id); ON DELETE/UPDATE optional." }
]} />

## Modifiers

<MappingTable targetLabel="Postgres" rows={[
  { sz: ".optional()", type: "nullable column", status: "ok", notes: "Same column as .nullable()." },
  { sz: ".nullable()", type: "nullable column", status: "ok", notes: "option and null both collapse to a nullable column." },
  { sz: ".array()", type: "T[]", status: "ok" }
]} />

## Escape hatch

For a value with no built-in mapping, store it explicitly with a Postgres type plus a Zod codec — see [field methods](/docs/reference/field-methods#escape-hatch).

<MappingTable targetLabel="Postgres" rows={[
  { sz: "s.$postgres(pgType, codec)", type: "pgType", status: "ok", notes: "Factory: any pg type + an encode/decode codec." },
  { sz: ".$postgres(wire, codec?)", type: "wire", status: "ok", notes: "Chainable: store an app value as the given wire type." }
]} />

## Not yet covered

These Postgres features are not expressible in the schema today, or can't be applied on the embedded engine. Do not rely on them yet:

- **Procedures** (`CALL`-only), **expression indexes** (an index on `lower(col)` rather than a plain column), and **`EXCLUDE` constraints.**
- **Multiple schemas** — everything lives in `public` — plus **roles and grants.**
- **Extension-backed types** — `defineExtension` can *declare* an extension, but the embedded PGlite engine bundles only a few, so citext / PostGIS / pgvector can't be *applied* locally (they work against a real Postgres server).

Native enums and domains, views and materialized views, functions, triggers, RLS policies, standalone sequences, composite / non-`id` foreign keys, and gin/gist/brin/hash + partial indexes are all supported — see [definers](/docs/reference/definers).

## Where to go next

- [Field methods](/docs/reference/field-methods) — the chainable methods on every field.
- [From schema to DDL](/docs/concepts/schema-to-ddl) — how these compose into DDL.
- [Definers](/docs/reference/definers) — `defineTable` and its table-level methods.
