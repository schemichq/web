// @ts-check
import { defineConfig } from "astro/config";

// schemic.dev - the main library hub + driver chooser. Phase 2 build-out;
// this is a placeholder scaffold for now.
export default defineConfig({
  site: "https://schemic.dev",
});
